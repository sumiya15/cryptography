#include <stdio.h>
int main() {
    unsigned char x = 0xAB; // binary 10101011
    int high = (x >> 7) & 1;
    int low = (x >> 3) & 1;
    printf("First bit of each nibble: %d %d\n", high, low);
    return 0;
}
